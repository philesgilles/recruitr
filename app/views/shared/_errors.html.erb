<% if obj.errors.any? %>
  <div class="alert alert-warning" role="alert">
    <h5><%= pluralize(obj.errors.count, "error") %> prohibited the creation of the article</h5>
    <ul>
      <% obj.errors.full_messages.each do |msg| %>
        <li>
          <%= msg%>
        </li>
      <% end %>
    </ul>
  </div>
<% end %>
